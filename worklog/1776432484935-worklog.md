# 演员选择模态窗列表模式改造工作日志

## 目标

在电影详情页(detail.html)和新增电影页(index.html)中，将演员选择模态窗(actor-selector-modal)从标签形式改为列表形式展示。

## 需求清单

1. 在演员选择模态窗中使用列表形式展示演员
2. 模态窗为上下结构：上部为搜索框，下部为演员列表，Footer为"确认"和"取消"按钮
3. 列表列包括：姓名、昵称、生日、备注，列表支持翻页，每页10条记录
4. 列表样式使用 scan-movies-list 相同的样式
5. 搜索框支持演员姓名和昵称搜索功能，搜索框样式使用 actor-filter-modal 的 search-box 相同样式
6. 被选择的条目采用边框高亮
7. 点击"确认"将选择的演员姓名填入 movie-actors-edit 中，点击"取消"关闭模态窗

## 工作过程

### 1. 修改 HTML 结构

#### 1.1 修改 detail.html

修改 `src/renderer/detail.html` 中的 actor-selector-modal 组件：
- 将 `.modal-content tag-selector-content` 改为 `.modal-content actor-selector-modal-content`
- 在 `.modal-body` 中添加搜索框容器 `.actor-selector-search`
- 将 `.tag-selector-list` 改为 `.actor-selector-list`
- 添加分页组件 `.actor-selector-pagination`

#### 1.2 修改 index.html

同步修改 `src/renderer/index.html` 中的 actor-selector-modal 组件，修改内容与 detail.html 相同。

### 2. 添加 CSS 样式

在 `src/renderer/css/main.css` 中添加演员选择列表相关样式：

```css
/* 演员选择模态窗内容容器 */
.actor-selector-modal-content {
    max-width: 900px;
    width: 900px;
    height: 600px;
    display: flex;
    flex-direction: column;
}

/* 搜索框样式 */
.actor-selector-search {
    padding: 15px 20px;
    border-bottom: 1px solid var(--border-color);
    flex-shrink: 0;
}

/* 演员选择列表容器（表格形式） */
.actor-selector-list {
    flex: 1 1 auto;
    overflow-y: auto;
    font-size: 13px;
    min-height: 0;
}

/* 表格样式（复用 scan-movies-list 样式） */
.actor-selector-list table {
    display: table;
    width: 100%;
    border-collapse: collapse;
    table-layout: fixed;
}

/* 表头、行、单元格样式 */
.actor-selector-list thead tr { ... }
.actor-selector-list th { ... }
.actor-selector-list tbody tr { ... }
.actor-selector-list tbody tr.selected { ... }  /* 选中行高亮 */
.actor-selector-list td { ... }

/* 分页组件 */
.actor-selector-pagination {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    padding: 12px 20px;
    border-top: 1px solid var(--border-color);
    background-color: var(--bg-secondary);
    flex-shrink: 0;
}
```

### 3. 修改 detail.js

#### 3.1 添加状态变量

```javascript
// 演员选择弹窗状态
let actorSelectorSearchKeyword = '';
let actorSelectorCurrentPage = 1;
const ACTOR_SELECTOR_PAGE_SIZE = 10;
```

#### 3.2 添加 DOM 元素引用

在 `elements` 对象中添加新的元素引用：
- `actorSelectorSearchInput`
- `actorSelectorSearchBtn`
- `actorSelectorClearBtn`
- `actorSelectorPrevBtn`
- `actorSelectorNextBtn`
- `actorSelectorPageInfo`

#### 3.3 重写 openActorSelectorModal 函数

```javascript
function openActorSelectorModal() {
    // 重置搜索和分页状态
    actorSelectorSearchKeyword = '';
    actorSelectorCurrentPage = 1;
    // 清空搜索框
    if (elements.actorSelectorSearchInput) {
        elements.actorSelectorSearchInput.value = '';
    }
    // 渲染演员列表
    renderActorSelectorList();
    elements.actorSelectorModal.style.display = 'flex';
}
```

#### 3.4 新增 renderActorSelectorList 函数

```javascript
function renderActorSelectorList() {
    // 根据搜索关键字过滤演员
    let filteredActors = actorsCache;
    if (actorSelectorSearchKeyword) {
        const keyword = actorSelectorSearchKeyword.toLowerCase();
        filteredActors = actorsCache.filter(actor => {
            const nameMatch = actor.name && actor.name.toLowerCase().includes(keyword);
            const nicknameMatch = actor.nickname && actor.nickname.toLowerCase().includes(keyword);
            return nameMatch || nicknameMatch;
        });
    }

    // 计算分页
    const totalActors = filteredActors.length;
    const totalPages = Math.ceil(totalActors / ACTOR_SELECTOR_PAGE_SIZE) || 1;

    // 截取当前页的数据
    const startIndex = (actorSelectorCurrentPage - 1) * ACTOR_SELECTOR_PAGE_SIZE;
    const endIndex = startIndex + ACTOR_SELECTOR_PAGE_SIZE;
    const pageActors = filteredActors.slice(startIndex, endIndex);

    // 生成表格HTML
    // ... 渲染表格结构

    // 更新分页信息
    updateActorSelectorPagination(totalPages);
}
```

#### 3.5 新增 updateActorSelectorPagination 函数

```javascript
function updateActorSelectorPagination(totalPages) {
    // 更新页码显示
    elements.actorSelectorPageInfo.textContent = `第 ${actorSelectorCurrentPage} / ${totalPages} 页`;
    // 更新翻页按钮状态
    elements.actorSelectorPrevBtn.disabled = actorSelectorCurrentPage <= 1;
    elements.actorSelectorNextBtn.disabled = actorSelectorCurrentPage >= totalPages;
}
```

#### 3.6 修改 toggleActorInSelection 函数

修改函数以支持表格行的选中状态切换：
```javascript
function toggleActorInSelection(actorName) {
    // ... 切换选中状态
    // 更新列表中的选中状态
    const row = document.querySelector(`#actor-selector-list tr[data-actor-name="${actorName}"]`);
    if (row) {
        if (selectedActors.includes(actorName)) {
            row.classList.add('selected');
        } else {
            row.classList.remove('selected');
        }
    }
}
```

#### 3.7 新增 escapeHtml 函数

添加 HTML 转义函数，防止 XSS：
```javascript
function escapeHtml(text) {
    if (!text) return '';
    const div = document.createElement('div');
    div.textContent = text;
    return div.innerHTML;
}
```

#### 3.8 添加搜索和分页事件绑定

在 `bindEvents` 函数中添加：
- 搜索按钮点击事件
- 清除搜索按钮点击事件
- 搜索框回车事件
- 上一页/下一页按钮点击事件

### 4. 修改 main.js

#### 4.1 添加状态变量

在 `state` 对象中添加：
```javascript
// 演员选择弹窗状态
actorSelectorSearchKeyword: '',  // 搜索关键字
actorSelectorCurrentPage: 1,  // 当前页码
actorSelectorPageSize: 10  // 每页记录数
```

#### 4.2 添加 DOM 元素引用

在 `elements` 对象中添加与 detail.js 相同的元素引用。

#### 4.3 重写相关函数

同步修改 `openActorSelectorModal`、`renderActorSelectorList`、`updateActorSelectorPagination`、`toggleActorInSelection` 等函数，与 detail.js 保持一致。

#### 4.4 添加搜索和分页事件绑定

在 `bindEvents` 函数中添加与 detail.js 相同的搜索和分页事件绑定。

## 工作结果

1. ✅ detail.html 和 index.html 中的 actor-selector-modal 已修改为列表模式
2. ✅ 添加了演员选择列表相关的 CSS 样式
3. ✅ detail.js 和 main.js 中的演员选择模态窗逻辑已重写
4. ✅ 实现了搜索功能（支持姓名和昵称搜索）
5. ✅ 实现了分页功能（每页10条记录）
6. ✅ 实现了选中行边框高亮效果
7. ✅ 单元测试通过（ActorService 17个测试全部通过）
8. ✅ 语法检查通过

## 单元测试结果

- ActorService 测试：17 个测试全部通过 ✅

## 待验证

- GUI 测试需要在实际应用中验证

---

## 样式修复记录 (2026-04-17)

### 问题描述

1. 用户要求 actor-selector-modal 使用 `modal-large` 样式
2. 演员表格样式没有生效

### 修复内容

#### 1. 修改 HTML 结构

将 `detail.html` 和 `index.html` 中的 actor-selector-modal 的 `.modal-content` 类名从：
```html
<div class="modal-content actor-selector-modal-content">
```
改为：
```html
<div class="modal-content modal-large actor-selector-modal-content">
```

#### 2. 修复 CSS 样式

- 移除 `.actor-selector-modal-content` 中的冲突尺寸属性，改为继承 `modal-large` 的基础结构
- 修复 `.actor-selector-list th.actor-selector-col-memo` 中无效的 `flex: 1` 属性，改为 `width: auto`
- 修复 `.actor-selector-list tbody tr.selected` 的边框样式，使用 `outline` 替代 `border` 以确保边框显示正确
- 为 `.actor-selector-list` 添加 `max-height` 限制，确保列表不会超出视口
- 为 `.actor-selector-list thead tr` 添加 `position: sticky` 使表头固定

### 修复后的 CSS 关键样式

```css
.actor-selector-modal-content {
    display: flex;
    flex-direction: column;
}

.actor-selector-modal-content .modal-body {
    flex: 1;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    padding: 0;
    min-height: 0;
    max-height: none;
}

.actor-selector-list {
    flex: 1;
    overflow-y: auto;
    font-size: 13px;
    min-height: 200px;
    max-height: calc(100vh - 380px);
}

.actor-selector-list tbody tr.selected {
    background-color: rgba(26, 115, 232, 0.15);
    outline: 2px solid var(--primary-color);
    outline-offset: -2px;
}
```